# Values for CI testing
cloudControllerManager:
  secret:
    name: binarylane-api-token

# Create a test secret for CI
createTestSecret: true

# Use a locally-built image in the kind cluster (loaded by CI)
image:
  repository: binarylane-cloud-controller-manager
  tag: local
  pullPolicy: Never
